import React from 'react';
import NavBar from './NavBar.jsx';

const Header = () => {
    return (
        <div className="row">
            <NavBar />
        </div>
    );
};

export default Header;